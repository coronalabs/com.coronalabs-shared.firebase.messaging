Sources for the plugin `shared.firebase.messaging`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.firebase.messaging" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
